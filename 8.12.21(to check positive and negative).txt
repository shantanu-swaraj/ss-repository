#include<stdio.h>
int main()
{
    int a;
    printf("enter a number= ");
    scanf("%d",&a);
 {
     if (a>0)
   printf("the number %d is positive",a);
 }
 {
     if (a<0)
     printf("the number %d is negative",a);
 }  
return 0;

}
